document.body.innerHTML = `
  <main style="max-width:720px;margin:64px auto;padding:24px;font:16px/1.6 system-ui;color:#172033">
    <h1>Codex-Router</h1>
    <p>This archived web interface no longer writes configuration files.</p>
    <p>Run <code>scripts/Start-CodexRouter.ps1</code> or open
    <code>Codex-Router.exe</code> from the portable package root.
    The desktop app stores credentials only in Windows Credential Manager.</p>
  </main>`;
