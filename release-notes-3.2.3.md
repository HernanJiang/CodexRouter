# CodexRouter 3.2.3

Antigravity 账号卡片现在会直接列出 Gemini 3.8 Flash。

3.2.2 只在 CLIProxy 实时目录已经返回 3.8 档位时才会折叠显示。本机当前返回的仍是 `gemini-3.7-flash-high` 等旧 ID，所以卡片上看不到 3.8。3.2.3 在账号已有 Gemini 模型时，把 **Gemini 3.8 Flash** 加进可选列表，加入后默认走 Medium 档。
