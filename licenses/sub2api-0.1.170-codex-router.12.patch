diff --git a/backend/internal/service/billing_service.go b/backend/internal/service/billing_service.go
index a43f26a8a..3cddb6c27 100644
--- a/backend/internal/service/billing_service.go
+++ b/backend/internal/service/billing_service.go
@@ -282,6 +282,10 @@ func (s *BillingService) initFallbackPricing() {
 		CacheReadPricePerToken: 0.15e-6,
 		SupportsCacheBreakdown: false,
 	}
+	// Antigravity documents Gemini 3.7 Flash as a subscription model but does
+	// not publish a separate token tariff. Reuse the preceding Flash rate only
+	// as a non-zero accounting fallback; dynamic pricing still takes priority.
+	s.fallbackPrices["gemini-3.7-flash"] = s.fallbackPrices["gemini-3.6-flash"]
 
 	// OpenAI GPT-5.4（业务指定价格）
 	s.fallbackPrices["gpt-5.4"] = &ModelPricing{
@@ -660,6 +664,9 @@ func (s *BillingService) getFallbackPricing(model string) *ModelPricing {
 	if strings.Contains(modelLower, "gemini-3.6-flash") || strings.Contains(modelLower, "gemini-3-6-flash") {
 		return s.fallbackPrices["gemini-3.6-flash"]
 	}
+	if strings.Contains(modelLower, "gemini-3.7-flash") || strings.Contains(modelLower, "gemini-3-7-flash") {
+		return s.fallbackPrices["gemini-3.7-flash"]
+	}
 
 	// DeepSeek V4 系列：仅匹配已知 V4 Pro/Flash 与官方兼容别名
 	// （deepseek-chat / deepseek-reasoner → V4 Flash），未知 deepseek-* 型号不回退，避免误计价。
diff --git a/backend/internal/service/billing_service_test.go b/backend/internal/service/billing_service_test.go
index 8f322b54e..c736f9edd 100644
--- a/backend/internal/service/billing_service_test.go
+++ b/backend/internal/service/billing_service_test.go
@@ -112,6 +112,23 @@ func TestGetModelPricing_FallbackMatchesByFamily(t *testing.T) {
 	}
 }
 
+func TestGetModelPricing_Gemini37FlashAliasesUseNonZeroFallback(t *testing.T) {
+	svc := newTestBillingService()
+
+	for _, model := range []string{
+		"gemini-3.7-flash",
+		"gemini-3.7-flash-low",
+		"gemini-3.7-flash-medium",
+		"gemini-3.7-flash-high",
+		"gemini-3-7-flash-medium",
+	} {
+		pricing, err := svc.GetModelPricing(model)
+		require.NoError(t, err, "model %s", model)
+		require.Positive(t, pricing.InputPricePerToken, "model %s input price", model)
+		require.Positive(t, pricing.OutputPricePerToken, "model %s output price", model)
+	}
+}
+
 func TestGetModelPricing_CaseInsensitive(t *testing.T) {
 	svc := newTestBillingService()
 
